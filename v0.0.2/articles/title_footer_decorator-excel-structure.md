# Excel structure for \`title_footer_decorator\`

This vignette documents the exact structure expected by
title_footer_decorator() for the Excel specification file. The decorator
expects a sheet that lists table identifiers and, for each table, a
TITLE row and zero-or-more FOOTNOTE rows.

The function expects the titles to be in the first sheet named “Sheet1”.
